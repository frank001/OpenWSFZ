# D001 Real-Signal Comparison — Baseline vs PCM-SIC Fix

| Field | Value |
|---|---|
| WSJT-X log | `WSJT-X ALL.TXT` |
| Baseline log | `baseline_all.txt` — pre-fix shim 20260002 (2-pass, no SIC) |
| Fix log | `OpenWSFZ ALL.TXT` — PCM-SIC shim 20260003 (3-pass, carrier subtraction) |
| WSJT-X total slots | 185 |
| Baseline slots | 184 |
| Fix slots | 176 |

## Recall vs WSJT-X — per decoder (own common slots)

Each decoder is compared against WSJT-X over the slots that *it* ran (not necessarily identical slot sets — crash-lost slots are excluded from the denominator for the affected decoder).

| Metric | Baseline | Fix | Δ |
|---|---|---|---|
| Common slots with WSJT-X | 184 | 176 | — |
| WSJT-X decodes (common) | 5,764 | 5,493 | — |
| Decoder decodes (common) | 3,404 | 3,192 | -212 |
| Matched (= TP) | 3,161 | 3,006 | -155 |
| **Recall of WSJT-X** | **54.8%** | **54.7%** | **-0.1 pp** |
| Decoder-only (bonus/FP) | 243 | 186 | -57 |

## Recall vs WSJT-X — three-way common slots only

Restricted to the 176 slots where all three logs have data (eliminates slot-count differences between baseline and fix).

| Metric | Baseline | Fix | Δ |
|---|---|---|---|
| WSJT-X decodes | 5,493 | 5,493 | — |
| Matched | 3,010 | 3,006 | -4 |
| **Recall** | **54.8%** | **54.7%** | **-0.1 pp** |
| Fix-only TP (fix found, baseline missed, WSJT-X confirms) | — | 106 | — |

## Slot coverage

| | Count |
|---|---|
| Slots in all three logs | 176 |
| Fix-only slots (not in baseline — crash losses etc.) | 0 |
| Baseline-only slots (not in fix) | 8 |

## Interpretation

- **Recall Δ (pp)** is the primary metric. Positive → fix improves real-signal decode rate.
- **Fix-only TP** counts messages that: (a) WSJT-X decoded, (b) the fix decoded, but (c) the
  baseline missed. These are the candidates attributable to the PCM-SIC extra passes.
- **Decoder-only** decodes are messages not in WSJT-X — a mix of genuine SIC bonus finds and
  false positives. Without an independent oracle these cannot be separated.

> **Note on hashed callsigns:** WSJT-X may decode `<HB10GBT>` (full hash) where OpenWSFZ
> reports `<...>` (generic placeholder). These will count as misses in both decoders even if
> the underlying message was correctly decoded. Recall figures are therefore lower-bounds.

---
_Generated by `qa/rr-study/compare_real_signal.py`_
