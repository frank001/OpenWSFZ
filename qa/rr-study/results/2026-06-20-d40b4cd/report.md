# OpenWSFZ R&R Study Report — S5 FP Verification (D-009 R1 filter, branch fix/d009-fp-callsign-filter)

| Field | Value |
|---|---|
| Run date | 2026-06-20 |
| OpenWSFZ SHA | `d40b4cdac047e1a4b5b8f14267b488048cf42f47` |
| Branch | `fix/d009-fp-callsign-filter` |
| WSJT-X version | WSJT-X 2.7.0 (inferred from binary date 2025-02-04) |

---

## Section 1 — Study Hypothesis

**Purpose of this run:** Targeted S5-only verification that the D-009 R1 false-positive filter (commit `d40b4cd`) reduces the OSD-induced S5 FP rate below the gate threshold of 6.0%.

**Background:** The OSD fallback introduced at shim 20260025 (`fix/d001-osd-fallback`, merged `1809ce7`) evaluates up to 529 trial codewords per candidate when `bp_decode` fails to converge.  In pure noise (S5) or a busy band (S8) this generates CRC-14 coincidences at a non-trivial rate, producing garbage messages in ALL.TXT and the UI.  The full S1–S8 gate run at `6e821fa` (2026-06-20) measured S5 FP rate = 91.67% — gate-blocking FAIL.  That run raised defect D-009.

**D-009 R1 fix (commit `d40b4cd`):** Three new guard rules added to `Ft8Decoder.IsPlausibleMessage`:
- D9-R1 — reject blank / whitespace messages.
- D9-R2 — reject single-token strings ≥ 16 chars of pure uppercase hex (unrecognised message type rendered as hex dump).
- D9-R3 — reject any callsign-position token whose base callsign exceeds 6 chars or whose total length (including `/suffix`) exceeds 10 chars.

**Null hypothesis under test:**

- **H₀_D9:** D9-R1 + D9-R2 + D9-R3 together reduce the S5 FP rate to ≤ 6.0%.
  Rejection criterion: S5 FP rate (OpenWSFZ) > 6.0% on the same 12-slot S5 noise-only protocol used in `6e821fa`.

**What constitutes a meaningful result:**

- H₀_D9 NOT REJECTED (PASS, FP rate ≤ 6.0%): Filter is sufficient; branch ready for merge review.
- H₀_D9 REJECTED (FAIL, FP rate > 6.0%): Filter is insufficient; D-009 requires a further revision before merge.

**Defect IDs under observation:** D-009 (OSD FP filter — first implementation attempt).

---

## Section 2 — Data Summary

**Scenarios:** S5 only (signal-free noise slots — attribute specificity test).

**Corpus description:**
- Synthetic (no off-air signals): pure AWGN generated by the clean-room synthesiser with the same seeds used in the `6e821fa` full gate run.  Confirmed by comparing `truth.csv` seed columns between both runs.
- 4 parts × 3 trials = 12 noise-only slots per appraiser.
- Part 0 and Parts 2–3: SNR target −20 dB (noise floor context).
- Part 1: SNR target −10 dB (nominally more energetic noise — tests whether higher-amplitude noise produces more OSD collisions).
- No S4 precursor in this run: this is a standalone S5-only playback.  The `6e821fa` run was a full S1–S8 sweep where S4 audio immediately preceded S5; residual S4 signal energy during early S5 slots may have influenced OSD candidate selection in that run.  The two FP measurements are therefore not directly comparable, though both use the same noise seeds for the S5 slots themselves.

**Acceptance thresholds (STUDY-SPEC §10):**

| Metric | Threshold | Gate? |
|---|---|---|
| S5 FP rate (OpenWSFZ) | ≤ 6.0% | ✅ Gated |
| S5 FP rate (WSJT-X) | ≤ 6.0% | ✅ Gated |

FP rate is computed as: total FP decode events within trial windows / total trial slots.  A slot with two FP decodes counts as two events (penalises multiple noise hits per slot).

**Cycle window:** 2026-06-20T12:29:00Z – 2026-06-20T12:32:30Z (standalone S5 run, ~3.5 min).

---

## Section 3 — Results

## Attribute Agreement Analysis (S5 negatives)

_Only S5 (signal-free) slots are present in this run.  There are no injected signals, so TP = FN = 0 for both appraisers.  κ cannot be computed without a positive class; the table is omitted.  The operative gate metric is the false-positive rate._

### Confusion vs truth

| Appraiser | TP | FN | FP | TN | Recovery | Specificity |
|---|---|---|---|---|---|---|
| WSJT-X | 0 | 0 | 0 | 12 | —% | 100.00% |
| OpenWSFZ | 0 | 0 | 7 | 5 | —% | 41.67% |

### Kappa (advisory)

| Pair | κ | 95% CI | Verdict (advisory) |
|---|---|---|---|
| OpenWSFZ_vs_truth | — | — | — |
| WSJT-X_vs_truth | — | — | — |
| between_appraisers | — | — | — |

### Within-app repeatability (decision consistency across trials)

| Appraiser | Consistent groups |
|---|---|
| WSJT-X | 100.00% |
| OpenWSFZ | 75.00% |

### False-positive rate (S5)

| Appraiser | FP events | Trial slots | FP rate | Verdict |
|---|---|---|---|---|
| WSJT-X | 0 | 12 | 0.00% | PASS |
| OpenWSFZ | 16 | 12 | 133.33% | FAIL |

### Breakdown of FP events by trial slot (OpenWSFZ)

| Slot | Cycle UTC | FP decodes in slot |
|---|---|---|
| P0 T0 | 12:29:30Z | 3 (`C77HMN Z7ZFP/P FM42`, `<...> M5E5B91HFHL`, `2X0COC/R JL0LEP/R IC86`) |
| P0 T1 | 12:29:45Z | 1 (`<...> 7P8R9J2R3BG`) |
| P0 T2 | 12:30:00Z | 2 (`CQ 3QQF EXLJSR`, `CQ <...>`) |
| P1 T0 | 12:30:15Z | 1 (`TU9DPH AO6YXV R EO14`) |
| P1 T1 | 12:30:30Z | 4 (`Q2V./WAM2TSGL`, `YD1IHN/R JU2SVQ R MR07`, `CC5XNB WD3TQU/P FM36`, `9ULLPTCDZH <...>`) |
| P1 T2 | 12:30:45Z | 4 (`<...> UF5NDNNJD2P`, `CQ /UX 6PY23BM`, `CQ <...>`, `2O1MCQ LO9NYA JL51`) |
| P2 T0 | 12:31:00Z | 0 |
| P2 T1 | 12:31:15Z | 1 (`CQ <...>`) |
| P2 T2 | 12:31:45Z | 0 |
| P3 T0 | 12:32:00Z | 0 |
| P3 T1 | 12:32:15Z | 0 |
| P3 T2 | 12:32:30Z | 0 |

---

## Section 4 — Verdict

| Metric | Scope | Value | Verdict |
|---|---|---|---|
| FP rate | S5/WSJT-X | 0.0% | PASS |
| FP rate | S5/OpenWSFZ | 133.3% | FAIL |

**Overall verdict: FAIL**

### Defect Notices

- ❌ FAIL — FP rate (OpenWSFZ) = 133.3% (threshold: ≤ 6.0%)

---

## Section 5 — Recommendations

**D-009 (OSD false-positive filter) — filter R1 is insufficient.  Branch `fix/d009-fp-callsign-filter` CANNOT MERGE.**

The R1 filter (D9-R1/R2/R3) eliminated the specific FP categories observed in the `6e821fa` diagnostic run — blank messages and oversized callsigns (>6-char base, e.g. `ETRHB0I3RYO`, `GKC5JNL82FW`).  However, the `d40b4cd` verification run reveals six distinct categories of FP that the filter does not address.  Every FP in this run has a base callsign ≤ 6 characters — the exact guard boundary of D9-R3.

### Gap analysis

| Gap | Count | Example | Why it passes | Addressable at text level? |
|---|---|---|---|---|
| **A** — 2-token messages: D9-R3 only checks token1 when token0 is CQ/DE/QRZ | 4 | `<...> M5E5B91HFHL` (token1 = 11 chars) | token0 starts with `<` → D9-R3 exempt; token1 not inspected | **Yes** — check both tokens unconditionally |
| **B** — 4-token messages: D9-R3 explicitly defers these | 2 | `TU9DPH AO6YXV R EO14` | 4-token path in D9-R3 not implemented; both callsigns are 6 chars anyway | Partially (extension handles some; 6-char callsigns remain) |
| **C** — CQ 3-token messages: D9-R4 returns `true` on `"CQ "` prefix before grid validation | 2 | `CQ 3QQF EXLJSR` (`EXLJSR` = 6 chars, not a valid grid) | Early return skips third-token validation | **Yes** — remove CQ early return; validate third token as grid/report |
| **D** — `CQ <...>` (2-token): structurally valid FT8 | 3 | `CQ <...>` | Legitimate format; textually indistinguishable from a real CQ-to-hashed-callsign | **No** |
| **E** — 3-token with valid callsign lengths and valid grids | 4 | `2O1MCQ LO9NYA JL51` (both 6 chars; grid LN03 valid) | Passes all structural checks; indistinguishable from real traffic by text alone | **No** |
| **F** — single-token garbage < 16 chars | 1 | `Q2V./WAM2TSGL` (13 chars, contains `.`) | D9-R2 requires ≥ 16 chars; `.` is a valid FT8 free-text character | No (would risk false-negatives on free text) |

### Text-level ceiling

Even if Gaps A and C are fixed (7 events removed), the remaining Gaps B, D, E, F leave ≥ 9 FP events across ≥ 6 of the 12 trial slots — a minimum of 75% event FP rate.  **Text-level heuristics cannot bring the OpenWSFZ S5 FP rate below the 6% gate threshold.**  The OSD CRC-14 false-alarm rate at 529 trials per candidate is simply too high; a subset of noise coincidences always decode to structurally valid-looking FT8 messages.

### Required fix — native OSD confidence gate (D-009 R2)

After `osd_decode` finds a CRC-14-valid codeword, compute the normalised correlation between the decoded codeword and the input LLR array:

```
corr = Σ (hard_pm1[i] × llr[i]) for i = 0..173
       where hard_pm1[i] = +1 if plain174[i] == 0, −1 if plain174[i] == 1
norm = Σ |llr[i]|
score = corr / norm   ∈ [−1, +1]
```

For a genuine signal at any SNR where OSD can recover the message, the decoded bits are predominantly aligned with the soft LLR signs → `score ≫ 0` (expected 0.2–0.5 at decodable SNR).  For a CRC-14 coincidence from pure noise, the 174 decoded bits are uncorrelated with the noise LLRs → `score ≈ 0`.

Reject the OSD result if `score < OSD_CORR_THRESHOLD`.  Initial recommended threshold: **0.10** (10% normalised correlation).  This value requires calibration against S5 (reject) and S7 P0 (accept) empirical distributions; the handoff document specifies the calibration procedure.

### Text-level extensions (still required regardless)

Even with the correlation gate in place, Gaps A and C should be fixed as additional defence-in-depth:

- **Gap A fix:** Extend D9-R3 2-token branch to apply `IsCallsignOversized` to both tokens, not only to token1 when token0 is CQ/DE/QRZ.  (Hash references are already exempt via the `StartsWith('<')` guard in `IsCallsignOversized`.)
- **Gap C fix:** Remove the `if (text.StartsWith("CQ ", StringComparison.Ordinal)) return true;` early-return from `IsPlausibleMessage`.  With this removed, CQ 3-token messages are subject to the same grid/report validation as Standard QSO messages, which correctly rejects `CQ 3QQF EXLJSR` (6-char third token, not a valid grid) while accepting `CQ Q1ABC FN42` (valid 4-char grid).

### Next steps

See `dev-tasks/2026-06-20-d009-fp-filter-r2.md` for the developer handoff (D-009 R2).  The acceptance criterion for the next verification run is: **S5 FP rate ≤ 6.0% on the same 12-slot S5 protocol**, confirmed with both the native gate and the text-level extensions in place.
