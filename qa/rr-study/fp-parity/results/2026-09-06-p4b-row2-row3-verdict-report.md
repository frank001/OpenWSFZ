# `FP-PARITY` P4b — ROW 2 / ROW 3 verdict

**QA, 2026-09-06 15:38Z** (`date -u`, HK-017). Spec:
`qa/rr-study/2026-09-06-1436-architect-to-qa-spec-fp-parity-p4b-row2-row3-verdict.md` (`FP-PARITY`
Amendment 4, commit `a9be754`, branch `arch/2026-09-06-fp-parity-p4b-spec`). Executed on branch
`qa/2026-09-06-fp-parity-p4b-execution`, built off my P3 execution tip (`c76efa9`) merged with the
spec branch — clean, no conflicts. Authorisation: `BOARD.md` 2026-09-06 15:03Z entry ("PO
AUTHORISED EXECUTION", relayed via the Architect session).

Script: `qa/rr-study/fp-parity/p4b_row2_row3.py` (new — did not exist before this session; built per
spec §1 by copying `row1_excess_floor.py`'s method, factored into a reusable population-computation
function rather than pasted twice, so the five-sweep and four-sweep leave-one-out figures cannot
silently diverge in method). Raw output: `qa/rr-study/fp-parity/p4b_row2_row3_results.txt`.

`git diff --stat -- src/ native/`: **empty** — verified on the merge and again before this report.
No new decode, no new sweep.

---

## 0. A data-provenance note, for the record

The raw `owsfz-all.txt` files for the five Amendment 1 A1.1 frozen sweeps are gitignored
(NFR-021 — real third-party callsigns) and therefore never travelled with the `git worktree` split:
they existed only in the Architect's physical checkout, not this QA worktree's. Copied across by
plain filesystem `cp` (never through git, never off this machine, landing in the same gitignored
path `qa/rr-study/results/<sweep>/owsfz-all.txt` these files already occupy) — done with the
Captain's explicit sign-off after Claude Code's own auto-mode classifier declined the action
unprompted. Line counts matched the already-committed record exactly (430/417/308/448/443), and a
plain re-run of the **unmodified** `row1_excess_floor.py` against the copies reproduced
`row1_excess_floor_results.txt` **byte-for-byte** (mechanically diffed, not asserted) before this
script touched anything. This is not a re-measurement — it is the same data that already produced
the committed ROW 1 result, now physically present where P4b needs to read it.

## 1. The mandatory guards, and that they are not decorative

Two STOP conditions are hard-coded in `p4b_row2_row3.py`, per spec §1/§2. Both were fault-injected
against a scratch copy of the script before this run was trusted (never against the committed
files):

- **Committed-result mismatch guard**: corrupting a copy of `row1_excess_floor_results.txt`'s `F`
  value from `+8.00` to `+9.00` and re-pointing the script at it produced `STOP`, exit code 1, and
  no verdict — confirmed the guard actually compares, rather than trusting its own recomputation.
- **Verdict-flip guard**: monkeypatching the four-sweep leave-one-out computation to return an
  artificially inflated `F = +9.40 dB` (which would flip `F − T` across the 6.0 dB bar) produced
  `STOP`, exit code 2, and no verdict — confirmed against the real code path, not just the
  arithmetic in isolation.

Neither guard fired on the real, uncorrupted run reported below.

## 2. Result

**Five-sweep `F` (Amendment 1 A1.1 frozen population): `+8.00 dB`.** Cross-checked against the
already-committed `row1_excess_floor_results.txt` — **F, p01, p05, the lowest-5 table, and the
14-row near-line table all match exactly.** `row1_excess_floor_results.txt` itself is unmodified by
this run (it is read-only input to the cross-check, not regenerated by this script).

**Four-sweep leave-one-out `F` (drops `2026-09-03-35378b9`, the only sweep on binary `20260050`,
per the era table derived — not hardcoded — from `AWGN-FP` ROW 0r + `FP-PARITY` Amendment 2 Ruling
2): `+8.00 dB`.** Unchanged from the five-sweep figure, and matching the by-hand figure already on
the board from the 2026-09-04 14:11Z ruling (`+8.00 dB` unchanged, `12/12` at −18 dB / `0/12` at
−21 dB over the four-sweep population) — now shipped as re-runnable code rather than resting on
that arithmetic alone.

**`T = 2.622 dB`**, cited not recomputed: `C = 1.622 dB` (base `FP-PARITY` spec §2, citing
`qa/rr-study/awgn-fp-replay/results/M1-M4-report.md` §4, n=454 offline false accepts) → `T = C + 1.0`
(base spec §4) → confirmed production-valid by `FP-PARITY` P3 ROW 0n-C
(`2026-09-06-1025-architect-to-qa-ruling-p3-adjudication.md` §2.4: `C' = +1.652 dB`,
`C' − C = +0.030 dB`, 0 of 432 rows exceed `T` — non-fire). Not re-opened by this row.

| | `F` | `F − T` | vs 6.0 dB bar |
|---|---|---|---|
| Five-sweep (A1.1 frozen population) | +8.00 dB | +5.378 dB | ROW 3 |
| Four-sweep leave-one-out (A1.3 disclosure) | +8.00 dB | +5.378 dB | ROW 3 |

**Both populations land on the same side of the bar — the verdict-flip guard does not fire.**

**Lowest 5 reconstructed (corrected) excess values, binary-attributed:**

| excess | reported SNR | injected rung | sweep | binary | cycle_utc |
|---|---|---|---|---|---|
| +8.00 dB | −18 dB | −18 dB | `2026-09-02-3b52608` | ≤20260049 | 2026-09-02T17:00:15Z |
| +8.00 dB | −18 dB | −18 dB | `2026-09-03-35378b9` | 20260050 | 2026-09-03T18:05:00Z |
| +9.00 dB | −17 dB | −18 dB | `2026-08-27-22b749c` | ≤20260049 | 2026-08-27T18:06:00Z |
| +9.00 dB | −17 dB | −18 dB | `2026-08-27-22b749c` | ≤20260049 | 2026-08-27T18:06:30Z |
| +9.00 dB | −17 dB | −18 dB | `2026-08-27-22b749c` | ≤20260049 | 2026-08-27T18:07:00Z |

Of the 14 decodes within ±1 dB of the 8.622 dB fire line, 3 were recorded on `20260050` and 11 on
`≤20260049` — full list with binary attribution in the raw output file. The knife-edge decode at
the fire line itself (`+8.00 dB`, two occurrences) is split one-and-one across the two eras, which
is exactly why A1.3 required this attribution: the verdict would not have moved either way (both
occurrences share the same `+8.00 dB` reading), but a future re-run that changed only one era's
population could not be assessed without this table.

## 3. Verdict

**ROW 3 fires.** Quoted verbatim from the base spec §4, per this spec's §4 step 4 mandate — no
softening:

> **ROW 3 — the margin is not there. FIRES iff `F − T < 6.0 dB`.** ⇒ No dev-task is authored. State
> plainly: *the emission-side floor cannot be set with the project's required margin on the evidence
> available, because the genuine population reaches down to within `F − T` dB of the false-accept
> ceiling.* 🛑 **PARKED, not closed** — closing the route requires a population that reaches the
> decoder's true floor, which S1b's bottom rung may not. Do **not** soften this into "needs more
> data" and do **not** re-run with a smaller margin.

No dev-task is authored. No filter is built, whichever row fired — this row's consequence is
purely a reporting one (spec §0(2)).

## 4. Standing disclosures, restated per spec §3

`F` stays labelled an **upper bound**, not the floor: bottom-rung (−18 dB) decodes at 100% pooled
(15/15) in the five-sweep population ⇒ the true floor is demonstrably lower and unmeasured by this
instrument (HK-026) — unchanged by this row, since neither population reaches the true floor.

## 5. HK-021 faults found in this spec

None. Predicate restated verbatim, not redrafted; split (5.378 vs 6.0) pre-registered before either
number existed (2026-09-03); both branches evaluated (five-sweep and four-sweep-LOO) and agree, so
this is not a decorative row — a disagreement was a live, fault-injection-tested possibility, not a
foregone formality.

## 6. Standing bars (unchanged)

No capture run proposed. No filter built. NFR-021: `p4b_row2_row3.py` and
`p4b_row2_row3_results.txt` scanned via the shipped scanner's own `scan()`/`classify()`, imported
directly (HK-022 — it cannot scan an uncommitted directory) — **0 hits, 0 shapes, both files.**
This report's own prose scanned by the same method before commit.
