## 1. Branch and ABI Preparation

- [x] 1.1 Create branch `feat/p15-iterative-subtraction` from `main`
- [x] 1.2 In `ft8_shim.h`: bump `FT8_SHIM_VERSION` from `20240001` to `20260001`
- [x] 1.3 In `ft8_shim.h`: add declaration for `ft8_get_last_pass_counts(int* out_counts, int capacity)`

## 2. C Shim — Iterative Subtraction Implementation

- [x] 2.1 In `ft8_shim.c`: add `#define K_MAX_PASSES 2` named constant (after existing K_* constants)
- [x] 2.2 In `ft8_shim.c`: add thread-local storage `tls_pass_counts[K_MAX_PASSES]` and `tls_num_passes` for per-pass stats
- [x] 2.3 In `ft8_shim.c`: add `suppress_candidate_tiles()` static helper — uses exact tone sequence from `ft8_encode()` (narrow suppression) plus ±1 bin to cancel Hann-window first sidelobes
- [x] 2.4 In `ft8_shim.c`: refactor the decode loop into a two-pass structure controlled by `K_MAX_PASSES`; first pass is the existing logic, second pass uses wider candidate net (K_MIN_SCORE_PASS2=1, K_MAX_CANDIDATES_PASS2=200, K_LDPC_ITERATIONS_PASS2=50)
- [x] 2.5 In `ft8_shim.c`: after each pass, collect successfully-decoded candidates + messages; suppress tiles before the next pass begins
- [x] 2.6 In `ft8_shim.c`: populate `tls_pass_counts[pass] = new_decodes` and `tls_num_passes = pass + 1` at the end of each pass
- [x] 2.7 In `ft8_shim.c`: implement `ft8_get_last_pass_counts()` — copy `tls_pass_counts` to `out_counts`, return `tls_num_passes` (clamped to `capacity`)
- [x] 2.8 In `ft8_shim.c`/`BUILD.md`: add `ft8_get_last_pass_counts` to all three platform link/export commands; add `ft8/encode.c` to compile lists (required for `ft8_encode()`)

## 3. Managed Interop Update

- [x] 3.1 In `Ft8LibInterop.cs`: update `ExpectedShimVersion` from `20240001` to `20260001`
- [x] 3.2 In `Ft8LibInterop.cs`: add `[DllImport] private static extern int NativeGetLastPassCounts([Out] int[] counts, int capacity)` P/Invoke declaration
- [x] 3.3 In `Ft8LibInterop.cs`: add `public static int[] GetLastPassCounts(int maxPasses)` public method
- [x] 3.4 In `Ft8Decoder.cs`: call `Ft8LibInterop.GetLastPassCounts(MaxDecodePasses)` after `DecodeAll`; `MaxDecodePasses = 2` is a named constant
- [x] 3.5 In `Ft8Decoder.cs`: log per-pass stats at Debug level: `"Iterative subtraction: pass {n} of {max}, {k} new decodes"`

## 4. Native Binary Rebuild

- [x] 4.1 Rebuild `libft8.dll` on Windows (MSVC 19.51, `native/ft8_lib` on `msvc-compat` branch); `ft8_get_last_pass_counts` confirmed in exports
- [x] 4.2 Copy rebuilt `libft8.dll` to `src/OpenWSFZ.Ft8/Native/win-x64/libft8.dll`
- [x] 4.3 Rebuild `libft8.so` on Linux (WSL2 GCC 14.2.0); three exports confirmed via `nm -D`
- [x] 4.4 Copy rebuilt `libft8.so` to `src/OpenWSFZ.Ft8/Native/linux-x64/libft8.so`
- [ ] 4.5 Rebuild `libft8.dylib` on macOS ARM64 — will be produced by CI on first push (macos-latest runner)
- [x] 4.6 Update `src/OpenWSFZ.Ft8/Native/win-x64/libft8.version.txt` with new build provenance (shim version 20260001, build date 2026-05-31)
- [x] 4.7 Update `BUILD.md`: msvc-compat branch reference, encode.c added, 3-symbol exports confirmed

## 5. Local Measurement — Replay Harness

- [x] 5.1 `dotnet build -c Release` → 0 errors, 0 warnings; ABI self-test passes
- [x] 5.2 Replay harness run against 42-cycle corpus
- [x] 5.3 **Recovery rate: 69.1% (613/887 matched); false-positive rate: 3.8% (24/637)**
       NOTE: AC-IS-1 target of ≥80% was NOT met. The spectrogram-domain approach plateaus
       at ~69% due to FFT-window sidelobe leakage that cannot be removed without PCM-domain
       waveform reconstruction. Sub-Hz carrier-frequency estimation is required for coherent
       PCM subtraction; this is proposed as a follow-up change. See design.md §Decisions §6
       for the detailed analysis.
- [x] 5.4 Iteration complete — ±1 bin suppression + LDPC=50 pass2 + K_MIN_SCORE_PASS2=1 is the best achievable result with current approach
- [x] 5.5 Timing: both passes complete in ~2 s combined (well within 13 s budget)

## 6. Fixture Answer-Key Expansion (AC-IS-2)

- [x] 6.1 Three committed fixtures show SAME matched counts as baseline (11/12/14) — no new signals recovered in these specific cycles via iterative subtraction
- [x] 6.2 No expansion required: no new signals to add (iterative subtraction gains were distributed across other cycles, not these three fixtures)
- [x] 6.3 N/A — no additions to verify
- [x] 6.4 G6 gate: 3 passed ✓

## 7. Full Test Suite Verification

- [x] 7.1 Full suite: 0 failed, 212 passed, 4 skipped (pre-existing synthetic fixture skips) ✓
- [x] 7.2 Performance timing: both passes complete ~2 s; well within 13 s / 30 s CI budget ✓
- [x] 7.3 Per-pass Debug log present in `Ft8Decoder.cs` — emits two lines: "Iterative subtraction: pass 1 of 2, N new decodes" and "pass 2 of 2, M new decodes"

## 8. Documentation and Findings Update

- [x] 8.1 `findings.md` regenerated by replay harness (69.1%, 613/887) — committed in this PR
- [x] 8.2 `findings.md` committed to branch
- [x] 8.3 `RECOVERY_PLAN.md` §7 Phase 2A updated: partially satisfied, 69.1% achieved, date 2026-05-31, change p15-iterative-subtraction

## 9. Commit and CI

- [ ] 9.1 Stage and commit all changes
- [ ] 9.2 Push branch; open draft PR; wait for CI green on all three legs
- [ ] 9.3 If G6 fails on macOS (binary rebuild needed): use CI workflow_dispatch to rebuild dylib

## 10. QA Review and Merge

- [ ] 10.1 QA review: `RECOVERY_PLAN.md` Phase 2A update and 69.1% recovery rate explanation
- [ ] 10.2 QA review: architectural analysis of why 80% was not achievable with Path A
- [ ] 10.3 After QA approval and CI green: mark PR ready-for-review; merge to `main`
- [ ] 10.4 Archive: `openspec archive p15-iterative-subtraction`
